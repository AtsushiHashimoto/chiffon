<p>
  % if ($object->{img}) {
    %= image url_for(qq{/$name/$object->{img}})->path->canonicalize, class => 'object-icon';
  % }
  %= $object->{name} // '';
  %= $object->{size} // '';
  %= $object->{amount} // '';
  %= $object->{unit} // '';
</p>
